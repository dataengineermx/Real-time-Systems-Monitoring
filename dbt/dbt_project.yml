name: 'real_time_systems_monitoring'
version: '1.0.0'
config-version: 2

profile: 'real_time_systems_monitoring'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  real_time_systems_monitoring:
    +materialized: view
